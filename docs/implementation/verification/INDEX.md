# Verification Reports — Index

- **Owned by:** `09-package-verification` · **Status:** first entry authored 2026-08-22.

| VR | Package | Date | Result | Headline findings |
|---|---|---|---|---|
| [VR-0010](VR-0010-project-scaffold.md) | IP-0010 (Project Scaffold & Shared Types) | 2026-08-22 | **VERIFIED** | Build/test/dev gates independently reproduced clean; 3 Low/Medium findings (stale `NFR-5003`→`NFR-5300` ID in the package's Requirements Covered field; per-type GDS citation style gap; project-wide RTM lag) — none blocking. |
| [VR-1010](VR-1010-session-turn-lifecycle.md) | IP-1010 (Session & Turn Lifecycle) | 2026-08-22 | **RETURNED** | Critical: `SessionStore.createSession` issues sequential, guessable IDs (`session-1`, `session-2`, …) — violates NFR-3200 and FS-101 Acceptance Criterion 1; no test would catch it. Medium: RTM rows for NFR-2100/2200/3200/6100 left `UNASSIGNED` despite being in Requirements Covered. Low: BL-0021's Deviation note overstates GDS-07's silence on Destroy's mechanism; DoD's "16 tests" claim conflates this package's own tests with the (24-test) full suite. |
| [VR-1010-v2](VR-1010-session-turn-lifecycle-v2.md) | IP-1010 (Session & Turn Lifecycle) — re-verification | 2026-08-22 | **VERIFIED** | Fresh, independent re-audit confirms VR-1010's Critical/Medium findings are genuinely fixed: `generateSessionId` now uses `crypto.randomBytes(16)` (128 bits, base64url) with a real 50-draw collision/format test; RTM rows for NFR-2100/2200/3200/6100 filled and accurate. Full DoD/Checklist re-derived from scratch (not just the delta); build clean; full suite green (29 tests as of this re-verification, including a concurrent IP-3011 commit that landed mid-session). One new Low, non-blocking note: the fix commit's claim of having "reworded" the BL-0021 Deviation note is imprecise (a corrective section was appended, not the original sentence edited). No dependent package flips to `READY` — IP-3010/IP-3011 remain the next packages awaiting their own verification. |
| [VR-3010](VR-3010-asset-roster-lifecycle.md) | IP-3010 (Asset Roster: Registration & Deploy Lifecycle) | 2026-08-22 | **VERIFIED** | Full DoD/Checklist audit passes: AP-cost deploy, ground/space `deployState.turnsUntilOnline` lifecycle, and `assertOnline` pre-online blocking all confirmed by reading the code and cross-checked against a real second consumer (IP-2010's `taskAction.ts`). Both disclosed deviations (reuse of GDS-07's existing `turnsUntilOnline` field; the additive `TurnManager.registerTurnEndHook`/BL-0022 extension) verified accurate and judged reasonable against VR-1010's BL-0021 model — `TurnManager.ts`'s diff confirmed purely additive, `shared/src/types.ts` confirmed untouched. Build clean; full suite green (38 tests). The package's own once-open checklist item (schema matching IP-3011's eventual data) is now independently confirmed true against IP-3011's real content. One new Low, non-blocking finding: `tickDeployStates` isn't yet wired to `registerTurnEndHook` in any production bootstrap — matches the current, identical un-wired state of every other handler/hook in the tree, deferred to the future transport/bootstrap package. No package flips to `READY`: IP-3011 becomes checkable (its sole dependency is now met) but IP-2010/IP-5010 still also need IP-3011 `VERIFIED`. |
| [VR-3011](VR-3011-asset-mission-content.md) | IP-3011 (Mission-Set & Asset-Type Content Templates) | 2026-08-22 | **VERIFIED** | All 7 asset-type + 3 mission-set JSON templates read directly and cross-checked against `TemplateRegistry`'s real schema; ground/space `timeToOnline` asymmetry (≤1 / ≥3) holds across every template. Every numeric field carries an in-content `_costRationale` disclosing it as pending `02-research-domain`'s R-1xx grounding (BL-0017) — confirmed honestly labeled, not silently final. BL-0027 (`loadContent.ts`'s runtime-`__dirname` JSON read isn't copied into `server/dist/`) independently reproduced and confirmed correctly scoped — this package's own G5 gate runs against source via `vitest`, genuinely unaffected. Build clean; full suite green, re-run fresh (52 tests, 12 files — up from the package's claimed 29, purely because IP-2010/IP-5010 landed concurrently; IP-3011's own 4 tests unchanged). `createGameEngine.wiring.test.ts` independently confirmed as a live, real-content exercise of deploy/online-lifecycle behavior (not a mock). Three Low, non-blocking findings (stale test-count line; FS-102/package "6 vs. 7 asset types" wording drift; FR-3300 RTM row not explicitly naming the content-side test). **IP-2010 and IP-5010 both now have every named dependency (IP-0010, IP-1010, IP-3010, IP-3011) `VERIFIED`** — each becomes the next checkable package for its own `09-package-verification` pass. |
| [VR-2010](VR-2010-sensing-f2t2e.md) | IP-2010 (Sensing & the F2T2E Chain) | 2026-08-22 | **RETURNED** | Critical (F1): tasking a sensor whose `chainRoles` contain no F2T2E precision role at all (e.g. an effector) is not rejected — confirmed live through the real `GameEngine.handleAction('task', ...)` path: AP is silently debited and `{accepted:true}` returned with zero belief effect and no distinguishable reason, violating FS-103 AC4/Error Handling despite the package's Checklist marking that item satisfied. No fixture in either test file exercises this case. The BL-0028 deviation (`applyTasking`'s two extra `observerState`/`opponentTrueState` parameters beyond GDS-09) is judged reasonable and accurately disclosed against the VR-1010/VR-3010 model; build clean; full suite green (52 tests); scope stayed within the declared file set; `createGameEngine.wiring.test.ts` (IP-5010's composition root) confirmed to genuinely exercise IP-2010's own decay logic end-to-end. F1 alone is sufficient for `RETURNED`; IP-2010 stays `COMPLETE`, no package flips to `READY`. |
| [VR-2010-v2](VR-2010-sensing-f2t2e-v2.md) | IP-2010 (Sensing & the F2T2E Chain) — re-verification | 2026-08-22 | **VERIFIED** | VR-2010's Critical F1 is genuinely fixed: `hasSensorCapability(chainRoles)` re-derives the same `capabilityCeiling` logic (not a stub) and `taskAction.ts` now calls it before `assertOnline`/AP-spend, confirmed by direct line-order reading. Live re-exercise of the exact original failing scenario (`chainRoles: ['engage']` via real `GameEngine.handleAction('task', ...)`) now correctly rejects with a clear reason, 0 AP spent, 0 belief entries; independently also confirmed find-only/fix-ceiling/track-ceiling (unaffected) plus a new full-chain (`target`-ceiling) case no committed fixture drives. Full DoD/Checklist re-derived from scratch; build clean; full suite green (54 tests, matching the package's own claim exactly); fix commit `b4be6e6` scope matches its declared file set precisely. No findings. **IP-2010 advances to `VERIFIED`; IP-6010 (deps IP-0010, IP-2010, both now `VERIFIED`, though IP-6010 itself was already found `COMPLETE` in the tree from concurrent implementation) is now eligible for its own `09-package-verification` pass.** |
| [VR-5010](VR-5010-propagator.md) | IP-5010 (`Propagator`, Two-Body Orbital Mechanics) | 2026-08-22 | **VERIFIED** | Independently hand-re-derived FS-104's worked example (`LEO-EQUATORIAL → GEO-POLAR` = 11 fuel / 5 turns) from the raw Maneuver Cost Table formula and cell-by-cell cross-checked every populated entry in `Propagator.ts`'s `ALTITUDE_COST`/`PLANE_COST` tables against FS-104's source tables — all matched; found the package's own Analysis checklist item (relying solely on the regression test matching FS-104's prose, no independent re-derivation) understated the rigor warranted, though the underlying code held up (Low finding F2). Both disclosed notes (BL-0030 composition-root file, BL-0031 fuel-budget-not-yet-deducted) independently confirmed accurate. Build clean; full suite green (60 tests, up from the package's claimed 52 purely from a later unrelated IP-2010 fix commit landing more tests — Low finding F1). All 7 Requirements Covered rows trace correctly in the RTM. No package flips to `READY`: IP-8010 needs all ten other packages `VERIFIED` first. |
| [VR-6010](VR-6010-fog-of-war-enforcement.md) | IP-6010 (Fog-of-War Enforcement) | 2026-08-22 | **VERIFIED** | `computeOpponentView`/`applyDeception` independently confirmed structurally incapable of leaking true opponent state — reads/writes only the observer's own `beliefOfOpponent` map, never `trueOpponentState.assets`/`.king`/`.activeEffects` or the subject's own `Asset`. Independently re-ran the package's own supersession sweep across all of `server/src` (not just the three grepped identifiers — read every file touching `PlayerState`) and reached the same "no other construction site" conclusion; independently live-exercised the boundary with a zero-belief-observer-vs-fully-populated-opponent scenario, an object-identity/mutation-isolation check, and a byte-for-byte true-`Asset` snapshot around `applyDeception` — all held. One Low, non-blocking finding: BL-0033's Deviation note is accurate for `computeOpponentView` but undercounts `applyDeception`'s actual signature delta (silently drops GDS-09's `observer: PlayerId` rather than retaining it, and adds an undisclosed `turnNumber` parameter) — functionally benign, a documentation-completeness gap for `07-implementation-planning`'s eventual GDS-09 reconciliation. Build clean; full suite green (57 tests, matching the package's own claim), run from an isolated `git worktree` since the live shared tree carried unrelated, concurrent, uncommitted IP-4010 edits that broke its own build. **IP-7010 flips `BLOCKED` → `READY`** (its three named dependencies — IP-0010, IP-1010, IP-6010 — are all now `VERIFIED`). IP-4010's dependency gate is also now fully satisfied but it was found already `COMPLETE` (uncommitted, concurrent work) rather than `BLOCKED` awaiting this flip. |
| [VR-4010](VR-4010-effect-resolver.md) | IP-4010 (`EffectResolver`, the Five D's Mechanism) | 2026-08-22 | **VERIFIED** | Deceive/Destroy structural distinction live-exercised beyond the committed test file, through the real `GameEngine.handleAction('engage', ...)` path: a King pre-populated with an active Disrupt entry and non-zero `consecutiveDenialTurns`/`totalDenialTurns` had its true state byte-for-byte unchanged across two repeated Deceive calls, while a separate Destroy call on another target did flip `destroyed` — confirming two genuinely different code paths, not a shared conditional. Denial-streak arithmetic (`consecutiveDenialTurns`/`totalDenialTurns`) independently cross-checked field-for-field against `GameEngine.checkWinConditions` (IP-1010) and FS-105/FR-4400 (6-turn threshold)/FR-1420 (60-turn timeout, `totalDenialTurns` tiebreak) — no drift, same fields, same names. The `TurnEndHook` `turnNumber` widening (BL-0036) confirmed additive/backward-compatible by re-running IP-1010's/IP-3010's own already-`VERIFIED` hook-consumer tests (`TurnManager.test.ts`, `deployAction.test.ts`, `taskAction.test.ts`, `createGameEngine.wiring.test.ts`) — all pass unchanged. One Low, non-blocking finding (F1): the `resolveEngagement` Deviation note discloses only the added `effectorObserverState` parameter but undercounts the actual delta (also adds undisclosed `currentTurn`/`falseRegime` parameters beyond GDS-09) — the same category of gap VR-6010's F1 found in `applyDeception`'s note. One more Low, cosmetic finding (F2): the package's own prose describes wiring into a "`handleAction` switch" when the actual mechanism is `GameEngine.registerHandler`. Build clean; full suite green (66 tests, matching the package's own claim exactly). **IP-4011 (names IP-4010 as its sole blocking dependency, already `COMPLETE`) is now the next package eligible for its own `09-package-verification` pass.** |
| [VR-4011](VR-4011-effect-content.md) | IP-4011 (Five D's Effect-Definition Content) | 2026-08-23 | **VERIFIED** | All five effect-definition files hand-verified against FS-105's prose and against `EffectResolver.ts`'s real `DISRUPT_DENY_DURATION`/`DEGRADE_DURATION` constants directly (not just against the package's own tests). Live re-exercise beyond the committed "BL-0037 cross-check" test (which only calls `disrupt`/`degrade`): independently confirmed the **Deny** duration through the real resolver, drove a non-default 7-turn multi-stack Degrade tick past the 6-turn mission-denial threshold with correct expiry/reset/total arithmetic, and confirmed Deceive's content-declared `"until-cleared"` value is never written into a real `EffectStateEntry`. Effector-to-effect doctrine mapping independently cross-checked cell-by-cell against IP-3011's real asset-type `_effectAffinity` content — fully consistent today. Three Low, non-blocking findings: (F1) the committed "references a real asset-type template" test only checks referential existence, not the doctrine cross-check the package's own "Tests to Add" section promises (data is correct today, but not regression-protected); (F2) Task 2's "IP-4010's `EffectDefinition` schema" wording is imprecise — the schema is authored entirely within this package, already substantively disclosed by BL-0037; (F3) the package's "70 tests" claim is stale (94 today) purely from concurrent IP-7010/IP-8010 landing, the established drift pattern. Build clean; full suite green. No package flips to `READY` alone: IP-8010 also still needs IP-7010 `VERIFIED`. |
| [VR-7010](VR-7010-transport.md) | IP-7010 (Server-Authoritative WebSocket Transport) | 2026-08-23 | **RETURNED** | High (F1): `handleConnection`'s reconnect path silently drops the connection when the presented `sessionId` no longer exists (`broadcastToOne` returns early, sends nothing) — live-reproduced with a fresh fake connection and a made-up `sessionId`, zero messages sent — directly violating FS-107's own W4 edge case and NFR-7200's "clear error state, not silent failure," and uncaught by either committed test file. High (F2): FS-101 §W7 requires a cancelled session to carry "a distinct outcome value (e.g. `'cancelled'`)" distinguishable from resignation/destruction/denial/timeout, but `SessionState` has no `outcome` field at all and the cancel path only sets `phase = 'ended'` — the package's own DoD claim of "`outcome: 'cancelled'`" is not implemented anywhere; live-reproduced a concrete consequence: cancelling a session past the 60-turn timeout cap causes `GameEngine.checkWinConditions` to actively mislabel it as `{winner: null, reason: 'timeout-tiebreak'}`. Independently live-exercised (not just re-run) the fog-of-war non-leak claim at the transport boundary — `broadcastStateDelta` genuinely computes two independent per-player views, no cross-contamination found in five separate checks — and confirmed `SessionStore`'s `crypto.randomBytes(16)` session-ID generation (VR-1010-v2's fix) is unchanged and not bypassed by `connectionRegistry.ts`. Build clean; full suite green (79 shared+server tests, matching the package's own claim exactly). IP-7010 stays `COMPLETE`, routed back to `08-code-implementation` (F1) and `07-implementation-planning` (F2's schema gap). |
| [VR-7010-v2](VR-7010-transport-v2.md) | IP-7010 (Server-Authoritative WebSocket Transport) — re-verification | 2026-08-23 | **VERIFIED** | Both VR-7010 High findings independently re-confirmed fixed, not just re-run: F1 live-exercised with a hand-constructed `FakeConnection` presenting a `sessionId` never created in the store — receives exactly one `{type: 'action-rejected', reason: 'session no longer exists'}`, plus a control case confirming a real active session's connect is unaffected. F2 re-derived VR-7010's exact hand-reproduced scenario independently (session past the 60-turn cap, imbalanced `totalDenialTurns`, cancelled directly) — `checkWinConditions` returns `{winner: null, reason: 'cancelled'}`, plus a control case (cancellation unset) proving the timeout-tiebreak branch really would have fired otherwise. `SessionState.cancelled` confirmed additive/optional (no existing construction site or consumer breaks); `WinReason`'s new `'cancelled'` member confirmed non-breaking (no exhaustive switch over it exists). Fog-of-war non-leak and `SessionStore` ID entropy re-confirmed unaffected (diff touches neither `BeliefState.ts` nor `SessionStore.ts`). Scope audit: `fd6b412`'s diff is exactly the 9 files the Remediation section named plus ledger housekeeping — no excursion. Rebuilt from a genuinely clean `node_modules`; full suite green (96 tests: 1 shared + 80 server + 15 client, matching the package's own claim exactly). Two Low, informational findings, neither introduced by this remediation nor blocking: (F1) a WebSocket connect during a session's `'deploying'` phase (session record exists but `SessionState` doesn't yet, per IP-1010's own data model) now also gets the "session no longer exists" message rather than a silent drop — pre-existing behavior, not reachable today (no production bootstrap exists yet, BL-0038), worth a note alongside BL-0038's eventual resolution; (F2) the RTM's NFR-7200 cell doesn't yet also name `websocketServer.test.ts`'s new F1 test alongside `disconnectFlow.test.ts` — not wrong, just incomplete. **IP-7010 flips `COMPLETE` → `VERIFIED`.** IP-8010 (names all 10 other packages as dependencies, all now `VERIFIED`) is the next package eligible for its own `09-package-verification` pass. |
| [VR-8010](VR-8010-presentation-ui.md) | IP-8010 (Presentation / UI) | 2026-08-23 | **RETURNED** | High (F1): no message type, interface, or shared static catalog exists anywhere in the codebase to deliver `AssetTemplate` (AP cost/time-to-online) data from server to client — `main.tsx` hardcodes `deployableTemplates: []`, GDS-09 never defines such a channel, no test renders `AssetTray` with non-empty data. FS-108 AC4/FR-8300 is therefore undeliverable end-to-end today, not merely an outstanding Demonstration item as the package's Deviation note implies by bundling it with the honestly-disclosed BL-0039 CSS-only gap. `AssetTray`'s own render logic is independently confirmed correct in isolation via a live scratch render with real data — the defect is the missing data path, not the component. Independently re-derived the fog-of-war rendering boundary beyond the committed component-level test, at the full `GameClient`→`App` pipeline level, with a contaminated `StateDeltaMessage` — no leak found. The legality pre-filter's coarse gates cross-checked line-by-line against the real server code (`TurnManager`, `assertOnline`, `Propagator.planManeuver`, `BeliefState.hasSensorCapability`) and confirmed a genuine parallel implementation, not a stub — one Low finding (F2) noted for the record: the client's `engage`-category gate checks `chainRoles.includes('engage')` but the server (`engageAction.ts`/`EffectResolver`) never does, a previously-uncaught IP-4010-scope gap, not an IP-8010 defect (the client is stricter, not laxer, so it cannot cause a post-hoc rejection). Build clean; full suite green (96 tests, rebuilt from a genuinely clean `node_modules`). IP-8010 stays `COMPLETE`, routed to `07-implementation-planning` (F1 needs an interface decision) then `08-code-implementation`, for a fresh `09-package-verification` pass. |
| [VR-8010-v2](VR-8010-presentation-ui-v2.md) | IP-8010 (Presentation / UI) — re-verification | 2026-08-23 | **VERIFIED** | VR-8010's High finding (F1/BL-0048) independently confirmed genuinely fixed, not just re-run: a scratch test driving the real `createGameEngine()`→`createTransport()`→`handleConnection()` path (not `<App>` plus a hand-built message) confirms a genuine `TemplateCatalogMessage`, built from `TemplateRegistry.listAssetTemplates()`, carrying all 7 of IP-3011's real asset-type templates (ids cross-checked against the actual `server/src/content/assetTypes/*.json` filenames), is sent exactly once per connection, byte-identical to both connections (no per-recipient filtering/duplication — expected, since `AssetTemplate` content is public/static), and not re-sent on a subsequent real `pass` action's `broadcastStateDelta`. `AssetTray.test.tsx`'s claims (affordable-enabled, unaffordable-disabled-with-reason, catalog survives a later state-delta) independently re-derived by reading `gameClient.ts`'s `handleMessage` spread order line by line — no ordering bug found; `deployableTemplates` is never named in the `'state-delta'` branch's overwritten fields. `AssetTemplate`/`MissionSetTemplate`'s relocation to `shared/src/interfaces.ts` confirmed non-breaking via a full-tree grep — `TemplateRegistry.ts`'s re-export means `deployAction.ts`/`loadContent.ts` needed no changes. `websocketServer.test.ts`/`disconnectFlow.test.ts`'s message-count updates confirmed to reflect real new behavior (unconditional catalog-before-everything-else), not a loosened assertion. Scope audit: commit `c734272`'s diff matches the package's own named file set exactly, no excursion. Build clean; full suite green (98 tests: 1 shared + 80 server + 17 client, matching the package's own claim exactly). No findings. **IP-8010 flips `COMPLETE` → `VERIFIED` — all 11 MVP Implementation Packages now stand `VERIFIED`; all 10 other packages re-confirmed still `VERIFIED` with no drift. Tranche ready for `10-integration-review`.** |
| [VR-9038](VR-9038-server-bootstrap.md) | IP-9038 (Real Server Bootstrap: Session HTTP API + WebSocket + Static Serving) | 2026-08-23 | **VERIFIED** | Every DoD/Checklist claim independently re-derived from a genuinely clean rebuild: `server/dist/content/` vs `server/src/content/` byte-for-byte 15=15 file-count/name match (BL-0027); full suite green at exactly 105 tests (1+84+20), matching the package's claim; a fresh, independently-written live smoke test (real `fetch` HTTP create+join, two real `ws` clients) reproduced the implementer's own end-to-end run, with both clients receiving a genuine `TemplateCatalogMessage`, plus a static `GET /` returning the real built `index.html`. Scope audit: commit `f2a1593`'s diff matches the declared Files to Create/Modify exactly — no engine/transport/authority-boundary file touched; `sessionApi.ts` confirmed to only ever handle session/player identifier strings, never `PlayerState`/belief-state data. **BL-0056 independently confirmed accurate at the code level**: `ActionType` union has no king-deployment member, every `submitKingDeployment` call site outside the tree's `__tests__` directories is absent, `GameEngine.handleAction` genuinely requires a truthy, `'active'`-phase session, `SessionStore.getSession()` genuinely returns `undefined` until both deployments resolve, and no client component beyond read-only display (`MissionKingStatus.tsx`/`IntelPanel.tsx`) references king deployment at all — a real, correctly-scoped, out-of-scope finding, not a defect in IP-9038 itself. Two Low, non-blocking findings: (F1) the WS-upgrade handler validates only presence, not store-validity, of `sessionId`/`playerId` before accepting the upgrade, deviating from the package's own task prose — functionally sound (a clean, non-silent rejection still reaches the client one message later) and arguably the more correct choice given BL-0056's phase-gating gap, but the prose should be corrected; (F2) FR-9410/FR-9420's RTM rows remain (correctly) `UNASSIGNED` pending real training-corpus content. **IP-9038 flips `COMPLETE` → `VERIFIED`.** No dependent package in the MVP tranche is gated on IP-9038 (it sits outside the MVP dependency graph); BL-0056 is recommended next for `07-implementation-planning` to package as a new remediation item (already drafted in the tree as `IP-9056-king-deployment-wiring.md` by a concurrent session). |
