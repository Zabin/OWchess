# Pipeline Backlog

Every finding, recommendation, Outstanding Issue, and Open Question a stage skill's run surfaces,
plus every feature request and bug report filed by `00-intake`, lands here as one row. IDs are
`BL-####`, sequential, never reused. Rows are appended or have their Status/Disposition updated in
place — never deleted; a rejected entry stays, marked `REJECTED` with the reason. Once a row flips
`DONE`/`REJECTED`, it moves to `docs/pipeline/backlog-archive.md` at the next triage sweep (see
`00-pipeline-manager`'s archiving convention) — that file does not exist yet because nothing has
been dispositioned to a terminal state.

**Disposition lifecycle:** `NEW` → `SCHEDULED` (names the step it rides with) / `DEFERRED` (names
its revisit trigger) / `NEEDS-USER` (names the exact decision required) / `REJECTED` (names the
reason) → `IN PIPELINE` → `DONE`. "We'll get to it" with no trigger is not a valid disposition.

**Writers:** `00-pipeline-manager` (harvest + triage + status flips) and `00-intake` (appends
`NEW` entries). No other stage skill writes this file directly.

| ID | Filed | Type | Summary | Sev/Pri | Entry stage | Disposition | Status |
|---|---|---|---|---|---|---|---|
| BL-0001 | 2026-08-21, GDS-01 (`docs/architecture/01-concept-of-operations.md`, OQ-11) | design-question | Whether maneuver/deploy transfer-time counts in the mover's own turns or in elapsed game-turns including the opponent's — ambiguous in GDS-01, needs a load-bearing decision before the data model can be built. | Medium | `03` | Resolved in GDS-03 (`docs/architecture/03-architecture.md`): counts in the mover's own turns only. | DONE |
| BL-0002 | 2026-08-21, GDS-01 (`docs/architecture/01-concept-of-operations.md`, OQ-12) | design-question | Whether passive detection (a player noticing they're being searched for, per SOR Appendix B's worked example) is a server-computed "detectability" signal or pure human inference from earned belief-state — affects whether `BeliefState`/`EffectResolver` need a detectability computation. | Medium | `03` | Resolved in GDS-03 (`docs/architecture/03-architecture.md`): pure human inference, no server mechanic in v1. | DONE |
| BL-0003 | 2026-08-21, GDS-04 (`docs/architecture/04-domain-model.md`, OQ-13) | research-gap | Exact orbital-regime/plane-class taxonomy (how many altitude bands, how many plane classes) needs grounding in what a real Kepler+J2 propagation naturally clusters into, before GDS-07 (Data Model) can pin down a concrete schema. | Medium | `02` | Resolved by R-203 (`docs/research/R-203-regime-plane-class-taxonomy.md`): recommends 9 named regimes (3 altitude bands x 3 plane classes). GDS-07 to formally adopt. | DONE |
| BL-0005 | 2026-08-21, R-203 (`docs/research/R-203-regime-plane-class-taxonomy.md`, §Sources) | research-gap | The J2 nodal-precession formula/coefficient cited in R-203 is single-sourced. | Low | `02` | DEFERRED — **moot for the v1 baseline** as of MSTR-001 C4 v0.3 (2026-08-22): the v1 `Propagator` uses two-body motion, no J2 term at all. Revisit trigger changed to: OQ-14 resolving toward *adding* J2 (Strategic Assumptions Register A8) — only then does this formula need cross-verification before implementation. | NEW |
| BL-0006 | 2026-08-21, `04-requirements-engineering` Review (RF-02, `docs/requirements/03-requirements-review.md`) | finding | NFR-1100 (3s turn-latency budget) and NFR-1200 (<100ms propagation-compute) are this pass's own reasoned first-guess values, not empirically grounded — no test harness exists yet to ground them in real data. | Low | `07`/`08` | DEFERRED — revisit trigger: a real latency-measurement harness exists (post `07`/`08`). | NEW |
| BL-0007 | 2026-08-21, `04-requirements-engineering` Review (RF-03) | finding | FR-6100 and NFR-3100 both state the fog-of-war boundary from two angles without cross-referencing each other. | Low | doc polish, `04`'s own docs | DEFERRED — revisit trigger: next revision of `01`/`02-non-functional-requirements.md`. | NEW |
| BL-0008 | 2026-08-21, `04-requirements-engineering` Review (RF-04) | finding | FR-8100–FR-8500 use Verification Method `Demonstration` without an inline justification sentence, though the skill's own rule requires justifying anything but `Test`. | Low | `06`/`07` | Resolved by FS-108's Verification Plan (`docs/features/FS-108-presentation-ui.md`): splits verification into Demonstration (visual/UX correctness — no snapshot-testing harness exists yet) vs. Test (action-menu pre-filter logic; fog-of-war rendering type-level boundary). | DONE |
| BL-0009 | 2026-08-21, `04-requirements-engineering` (CR-01); ripened 2026-08-22 by FS-103 | design-question | Belief-state decay rate (turns before a precision downgrade). | Medium | `06` | Resolved by owner 2026-08-22: 5-turn window; `'find'`-level entries removed (not floored) when stale. Propagated to FR-2300, FS-103, RTM, feature catalog. | DONE |
| BL-0010 | 2026-08-21, `04-requirements-engineering` (CR-02); ripened 2026-08-22 by FS-101 | design-question | Disconnect/reconnect grace-period duration and forfeit-on-timeout policy. | Medium | `06` | Resolved by owner 2026-08-22: no grace period/timeout; notify-and-choose (wait indefinitely or cancel). Propagated to FR-7300, FS-101 §W7, RTM, feature catalog, release plan, assumptions register. | DONE |
| BL-0011 | 2026-08-21, `04-requirements-engineering` (CR-03); resolved 2026-08-22 | research-gap | Per-regime-pair maneuver fuel-analog budget and transfer-time-in-turns table. Resolved: FS-104's new Maneuver Cost Table (`docs/features/FS-104-orbital-mechanics-propagator.md`), adopting R-201's two-body Δv/time-of-flight figures into a concrete altitude/plane/combined-maneuver formula. Propagated to FR baseline (CR-03 struck), RTM, feature catalog. | Medium | `06` | Resolved — see FS-104's Maneuver Cost Table. | DONE |
| BL-0012 | 2026-08-22, FS-101 (`docs/features/FS-101-session-turn-lifecycle.md`, Open Questions); resolved 2026-08-22 | design-question | Win-condition check ordering when King destruction and the 60-turn timeout/tiebreak could both fire in the same check. | Low | `07` | Resolved by IP-1010 (`docs/implementation/packages/IP-1010-session-turn-lifecycle.md`): destruction checked and returned first, confirming FS-101's own default — no objection raised. | DONE |
| BL-0013 | 2026-08-22, FS-102 (`docs/features/FS-102-asset-roster-mission-sets.md`, Open Questions); resolved 2026-08-22 | design-question | Whether per-template deploy limits exist or an unlimited count bounded only by AP scarcity is the deliberate v1 design. | Low-Medium | `04`/`06` | Resolved by IP-3010 (`docs/implementation/packages/IP-3010-asset-roster-lifecycle.md`): v1 ships with no per-template cap, bounded only by AP scarcity — the lower-risk, less-scope-adding MVP default; easy to add a cap later without a data-model change if playtesting shows it's needed. | DONE |
| BL-0014 | 2026-08-22, FS-104 (`docs/features/FS-104-orbital-mechanics-propagator.md`, Open Questions); resolved 2026-08-22 | design-question | Whether a second maneuver request while one is already in-progress is rejected, queued, or replaces the first. | Low | `07` | Resolved by IP-5010 (`docs/implementation/packages/IP-5010-propagator.md`): rejected outright, confirming FS-104's own assumption (GDS-07's single-field `maneuverState` schema) — no objection raised. | DONE |
| BL-0015 | 2026-08-22, FS-105 (`docs/features/FS-105-effect-resolution.md`, Open Questions); resolved 2026-08-22 | design-question | Whether the King denial-streak counter is a stored field or recomputed from `activeEffects` each check. | Low | `07` | Resolved by IP-4010 (`docs/implementation/packages/IP-4010-effect-resolver.md`): stored field (`consecutiveDenialTurns`) on the King asset, chosen for O(1)-read simplicity — no behavioral difference either way, per FS-105's own framing. | DONE |
| BL-0016 | 2026-08-22, `07-implementation-planning` (IP-4010's authoring pass) | finding | `BeliefState.applyDeception` (GDS-09) had no package owner in the initial Technical Work Breakdown pass — surfaced when IP-4010 tried to cite it as a dependency. | Low | `07` | Resolved same run — assigned to IP-6010 (`docs/implementation/packages/IP-6010-fog-of-war-enforcement.md`), which already owns `BeliefState`'s mutation surface for the enforcement boundary; IP-4010's dependency edge updated accordingly. | DONE |
| BL-0017 | 2026-08-22, `07-implementation-planning` (IP-3011/IP-4011's authoring pass) | research-gap | The v1 content templates (asset-type AP costs/regime affinities, effect-to-effector capability mapping) need `02-research-domain`'s doctrine grounding (R-1xx tier), which is not yet authored — IP-3011/IP-4011 can proceed with reasoned-but-placeholder costs, but `09-content-review`'s doctrinal-coherence check depends on this grounding existing first. | Medium | `02` (R-1xx) | SCHEDULED — `02-research-domain` before `09-content-review` closes IP-3011/IP-4011; does not block `08-content-authoring` from starting with reasoned interim values. | NEW |
| BL-0004 | 2026-08-21, GDS-06 (`docs/architecture/06-non-functional-requirements.md`) | finding | NFR-4002 (never show an action as available then reject it) vs. server-authoritative-only design: satisfying it without added latency needs bounded, intentional client-side legality-rule duplication (kept in sync via the shared-types mechanism from ADR-0001), not a bare ask-then-render. | Low | `03` (GDS-08/09) | Resolved in GDS-08 (`docs/architecture/08-presentation-architecture.md`): bounded, read-only client-side legality-rule copy from the shared-types package, server still validates independently. | DONE |
| BL-0018 | 2026-08-22, VR-0010 (`docs/implementation/verification/VR-0010-project-scaffold.md`, finding M-1) | doc-defect | IP-0010 cites `NFR-5003`, a retired SOR-era ID — the current baseline renumbered it to `NFR-5300`. Code satisfies the live requirement; only the package's own citation is stale. | Low-Medium | `07` | SCHEDULED — cheap fix to IP-0010's Requirements Covered field, rides with the next `07-implementation-planning` touch (or a dedicated doc-fix pass); non-blocking. | NEW |
| BL-0019 | 2026-08-22, VR-0010 (finding L-2) | doc-defect | `shared/src/{types,interfaces,messages}.ts` carry file-level GDS citations, not the per-type inline citations IP-0010 Task 2 literally asked for (traceability itself independently confirmed correct by VR-0010). | Low | `08` | DEFERRED — cosmetic; revisit if a future package's own file touches these headers anyway, not worth a dedicated pass on its own. | NEW |
| BL-0020 | 2026-08-22, VR-0010 (finding L-3) | doc-defect | The requirements-level RTM (`docs/requirements/04-requirements-traceability-matrix.md`) generally lags behind landed packages — pre-existing, not IP-0010-specific. | Low | `04`/`00` | Superseded — IP-1010's own implementation pass updated the FS-101 rows directly (§below); adopted as the new per-package norm rather than a deferred batch pass. | DONE |
| BL-0021 | 2026-08-22, IP-1010 implementation pass | finding | GDS-07's `Asset` shape had no field for cumulative (lifetime) denial-turns needed by FR-1420's timeout/tiebreak rule, distinct from `consecutiveDenialTurns` (BL-0015). `destroyed: boolean` was also added (Destroy's removal-vs-flag choice, GDS-07 leaves either as valid; a flag fits the single-King-field `PlayerState.king` shape). | Low-Medium | `07`/`03` | Resolved same run — `totalDenialTurns`/`destroyed` added to `shared/src/types.ts` directly (additive, non-breaking); recorded here for `07-implementation-planning`/GDS-07 to formally reflect in the next data-model touch. | DONE |
| BL-0022 | 2026-08-22, IP-3010 implementation pass | finding | Ticking `deployState.turnsUntilOnline` down each turn needed a turn-scoped hook `TurnManager` (IP-1010) didn't expose — a verb-inventory gap (the "persist"/tick verb for deploy-lifecycle had no named owner across turn-advances) parallel to BL-0016's `applyDeception` gap. | Low-Medium | `07` | Resolved same run — added `TurnManager.registerTurnEndHook` (small, additive, non-breaking extension of a file IP-3010 doesn't list as its own); `07-implementation-planning` should note this general pattern (Propagator/BeliefState/EffectResolver will need the same hook) so IP-5010/2010/4010 don't each reinvent it. | DONE |
| BL-0023 | 2026-08-22, VR-1010 (`docs/implementation/verification/VR-1010-session-turn-lifecycle.md`, finding F1) | bug | **Critical**: `SessionStore.createSession` generated sequential, guessable session IDs (`session-1`, `session-2`, ...), violating NFR-3200 (≥122 bits entropy) and FS-101 Acceptance Criterion 1. | Critical | `08` | Fixed same day — `crypto.randomBytes(16)` base64url IDs, with a collision/format regression test. IP-1010 re-submitted for a fresh `09-package-verification` pass. | DONE |
| BL-0024 | 2026-08-22, VR-1010 (finding F2) | doc-defect | RTM rows for NFR-2100/2200/3200/6100 left `UNASSIGNED` despite being in IP-1010's own Requirements Covered. | Medium | `08` | Fixed same day — all four rows filled (structural/inspection notes for NFR-2100/2200/6100; a real test for NFR-3200 alongside the F1 fix). | DONE |
| BL-0025 | 2026-08-22, VR-1010 (finding F3) | doc-defect | BL-0021's Deviation note overstated GDS-07's silence on Destroy's mechanism — GDS-07's Merge Gate commits to array-removal for the general case; the King's flag is a reasonable carve-out for its distinct single-field storage, not evidence GDS-07 was silent generally. | Low | `07` | Fixed same day — IP-1010's Deviation note reworded to state the King-specific carve-out precisely, per VR-1010's own judgment. | DONE |
| BL-0026 | 2026-08-22, VR-1010 (finding F4) | doc-defect | IP-1010's Verification Checklist conflated "this package's N tests" with "the full suite's M tests" (claimed 16, full suite was 24). | Low | `08` | Fixed same day — reworded to distinguish this package's own test count from the full-suite count explicitly. | DONE |
| BL-0027 | 2026-08-22, IP-3011 implementation pass | finding | `loadContent.ts` reads content JSON at runtime relative to its own compiled location; `tsc -b` doesn't copy `.json` files into `server/dist/`, so a server run from built output (not yet exercised by any package) would not find its content without an added build step. | Low-Medium | `07`/`08` | SCHEDULED — rides with whichever package first actually starts the server from `dist/` (likely `IP-7010`'s transport work); this package's own G5 gate (build + `npm test`, which runs against source) is unaffected. | NEW |
| BL-0028 | 2026-08-22, IP-2010 implementation pass | finding | GDS-09's `BeliefState.applyTasking(observer, sourceAsset, targetRegime, turnNumber)` has no parameter for the observer's own `PlayerState` or the opponent's true state — both needed to resolve tasking meaningfully. Implemented with two additional parameters rather than reaching into a session store from inside `BeliefState`. | Low-Medium | `07`/`03` | SCHEDULED — `07-implementation-planning`/GDS-09 to formally reconcile the interface signature next time GDS-09 is touched; additive, non-breaking, does not block downstream packages. | NEW |
| BL-0029 | 2026-08-22, VR-1010-v2 (`docs/implementation/verification/VR-1010-session-turn-lifecycle-v2.md`, finding F5) | doc-defect | IP-1010's fix commit/BL-0025 claimed the Deviation note's overstated GDS-07 characterization was "reworded," but the original sentence was left byte-for-byte unchanged — only a new "Post-verification fix" section was appended below it. | Low | `08` | Fixed same run — the original sentence itself now states the King-specific removal-vs-flag carve-out precisely, not just the separately-appended fix section. | DONE |
| BL-0030 | 2026-08-22, IP-5010 implementation pass; independently confirmed as VR-3010's finding F1 | finding | No file wired any package's turn-end hooks (deploy-state ticking IP-3010, belief decay IP-2010, maneuver ticking IP-5010) to `TurnManager.advanceTurn()` outside of tests — each built in isolation, none composed into a runnable path, since no server bootstrap exists yet. | Low-Medium | `07`/`08` | Resolved same run — added `server/src/engine/createGameEngine.ts` as the minimal composition root, proven end-to-end by `createGameEngine.wiring.test.ts`. `07-implementation-planning` should note `IP-7010` (transport) is expected to extend this file as the real production bootstrap, not reinvent it. | DONE |
| BL-0031 | 2026-08-22, IP-5010 implementation pass | design-question | `maneuverAction.ts` spends a flat 1 AP (FR-5300) but doesn't yet deduct from a per-asset fuel-analog budget — no template/`Asset` field tracks one yet, though `Propagator.planManeuver`'s `fuelCost` return is computed and available. | Low-Medium | `07`/`06` | SCHEDULED — decide where a fuel-budget field belongs (template-level cap vs. per-asset running total) next time FS-104/IP-5010 is revisited; not blocking MVP play (AP already gates maneuver frequency). | NEW |
| BL-0032 | 2026-08-22, VR-2010 (`docs/implementation/verification/VR-2010-sensing-f2t2e.md`, finding F1) | bug | Tasking with an effector-only asset (`chainRoles: ['engage']`, no F2T2E sensing role) through `GameEngine.handleAction('task', ...)` silently succeeded — 1 AP debited, no belief entry, no rejection — violating FS-103 Acceptance Criterion 4/Error Handling. | High | `08` | Fixed same day — exported `hasSensorCapability` from `BeliefState.ts`; `taskAction.ts` now rejects before spending AP, with a regression test distinguishing this case from the legitimate ceiling-reached no-op refresh. IP-2010 re-submitted for a fresh `09-package-verification` pass. | DONE |
| BL-0033 | 2026-08-22, IP-6010 implementation pass | finding | GDS-09's `computeOpponentView`/`applyDeception` signatures both lack a parameter for the observer's own `PlayerState` — the same root cause BL-0028 found in `applyTasking` (GDS-09's pseudocode omitted every `BeliefState` method's actual state-access needs). | Low-Medium | `07`/`03` | SCHEDULED — `07-implementation-planning`/GDS-09 to reconcile all three signatures (`applyTasking`, `computeOpponentView`, `applyDeception`) together next time GDS-09 is touched, rather than three separate patches; additive, non-breaking, does not block downstream packages. | NEW |
| BL-0034 | 2026-08-22, VR-5010 (`docs/implementation/verification/VR-5010-propagator.md`, finding F1) | doc-defect | IP-5010's own Verification Checklist test-count claim ("52 total... incl. this package's 11") went stale after later commits added more tests — same class of drift VR-1010-v2/VR-3010/VR-3011 all flagged and treated as non-blocking expected growth. | Low | `08` | DEFERRED — same disposition precedent as the identical finding on IP-1010/IP-3010/IP-3011; not worth a dedicated fix pass, correctly noted as expected growth each time it recurs. | NEW |
| BL-0035 | 2026-08-22, VR-5010 (finding F2) | finding | IP-5010's own Verification Checklist "Analysis" item declined to independently re-derive the Maneuver Cost Table's figures beyond one worked-example regression test — weaker than it should be, since a single hardcoded test can't catch a table-wide, self-consistent implementer error. VR-5010 itself performed the missing cell-by-cell hand re-derivation (all 24 populated table entries) and found no discrepancy. | Low | `07` | SCHEDULED — tighten the Analysis checklist wording for future orbital/formula-table packages to require a cell-by-cell or formula-level re-derivation, not just one worked-example regression test; non-blocking since the underlying code was independently confirmed correct this time. | NEW |
| BL-0036 | 2026-08-22, IP-4010 implementation pass | finding | `TurnManager.TurnEndHook`'s signature (added by IP-3010, BL-0022) only passed the ending player's state, not the current turn number — `EffectResolver.tickActiveEffects` needs it to compute elapsed effect duration. | Low | `07` | Resolved same run — added `turnNumber` as a second hook parameter (small, additive, backward-compatible: existing single-argument hooks are unaffected in JS/TS). Filed for `07-implementation-planning`/GDS-09 to note alongside BL-0028/0033's similar state-access-parameter gaps. | DONE |
| BL-0037 | 2026-08-22, IP-4011 implementation pass | design-question | `EffectResolver.ts` (IP-4010) uses hardcoded duration constants, not IP-4011's new `EffectDefinitionRegistry` content — the two are cross-checked by a dedicated test (`effectDefinitions.test.ts`'s "BL-0037 cross-check") that would fail on drift, but nothing else keeps them in sync. | Low-Medium | `07` | SCHEDULED — decide whether a future package should make `EffectResolver` genuinely content-driven (read from the registry) or whether hardcoded-constants-plus-cross-check-test is an acceptable permanent v1 design; non-blocking, both are internally consistent today. | NEW |
| BL-0038 | 2026-08-22, IP-7010 implementation pass | finding | No package yet bootstraps a real `WebSocketServer` wrapping actual sockets into the `Connection` interface and calling `createTransport`/`handleConnection` — `server/src/index.ts` remains a scaffold placeholder. The transport's own game logic is fully implemented/tested against a fake connection. | Low-Medium | `07`/`08` | SCHEDULED — the last remaining piece before the server can run end-to-end; likely rides with whichever package first needs a real running server (integration testing, `10-integration-review`, or a dedicated small bootstrap package), not blocking any MVP package's own G5 gate. | NEW |
| BL-0039 | 2026-08-23, IP-8010 implementation pass | finding | IP-8010 implements the full component/state/legality logic FS-108 describes (all Test-verifiable criteria passing) but ships no visual styling (CSS, layout, ZabOW-reference palette/composition) — components render semantic markup + `data-testid` hooks only. FS-108's own Verification Plan treats visual/UX correctness (Demonstration) as load-bearing, not optional polish. | Medium | `08`/`09-content-review` | SCHEDULED — a follow-up styling pass is needed before FS-108 can be considered fully done against its own bar; not blocking this package's own G5 gate (build/test), which is genuinely green. | NEW |
| BL-0040 | 2026-08-23, session-limit interruption during 09-package-verification dispatch | gate | Three verification agents (IP-4011, IP-4010 re-check, IP-7010) failed mid-run with "session limit · resets 2:20am (UTC)" — background-agent capacity was exhausted for this account. IP-4010 itself was independently confirmed VERIFIED via VR-4010 before the limit hit; IP-4011/IP-7010/IP-8010 remain `COMPLETE`, unverified. | Medium | `09` | Resolved run #43 (2026-08-23) — the reset window passed; fresh independent verification agents for IP-4011 and IP-7010 dispatched successfully and are running without hitting the limit. | DONE |
| BL-0041 | 2026-08-23, VR-4011 (`docs/implementation/verification/VR-4011-effect-content.md`, finding 1) | finding | The committed test asserting `allowedEffectorTemplateIds` reference real templates only checks referential existence — it doesn't enforce the doctrine cross-check ("no effector applies an effect it shouldn't") that IP-4011's own "Tests to Add" section promises. Data is correct today; not regression-protected. | Low | `08-content-authoring` | SCHEDULED — add the real doctrine-mapping regression test next time IP-4011's content is touched; non-blocking. | NEW |
| BL-0042 | 2026-08-23, VR-4011 (finding 2) | doc-defect | IP-4011's Task 2 wording ("validate against IP-4010's `EffectDefinition` schema") is imprecise — that schema is authored entirely within IP-4011 itself, not IP-4010. | Low | `07` | DEFERRED — cosmetic wording fix; rides with next `07` touch of IP-4011. | NEW |
| BL-0043 | 2026-08-23, VR-4011 (finding 3) | doc-defect | IP-4011's "70 tests" claim is stale (94 today) purely from concurrent IP-7010/IP-8010 landing after IP-4011 was authored. | Low | `08-content-authoring` | DEFERRED — cosmetic drift, expected from concurrent package landing; not worth a dedicated fix pass. | NEW |
| BL-0044 | 2026-08-23, VR-7010 (`docs/implementation/verification/VR-7010-transport.md`, finding F1) | bug | **High**: `handleConnection`'s reconnect path silently drops the connection when the presented `sessionId` no longer exists — `broadcastToOne` returns early with no message sent at all, violating FS-107's own named W4 edge case and NFR-7200 (no silent failure). | High | `08-code-implementation` | Fixed and independently re-verified 2026-08-23 (VR-7010-v2): `broadcastToOne` now sends an explicit `action-rejected`/`'session no longer exists'` rejection instead of a silent drop, live-reproduced as genuinely fixed (not just re-running the committed test). | DONE |
| BL-0045 | 2026-08-23, VR-7010 (finding F2) | bug | **High**: `SessionState` has no `outcome`/distinct-cancellation field at all — IP-7010's own DoD claims `'cancel'` produces `outcome: 'cancelled'`, but no such value exists anywhere in the schema or shipped code. Live-reproduced consequence: cancelling a session past the 60-turn timeout cap causes `GameEngine.checkWinConditions` to mislabel it `{reason: 'timeout-tiebreak'}` instead of a cancellation. | High | `07-implementation-planning` (schema gap) then `08-code-implementation` (IP-7010 wiring) | Fixed and independently re-verified 2026-08-23 (VR-7010-v2): `SessionState.cancelled` (additive) + `WinReason: 'cancelled'`, checked first in `checkWinConditions` — re-derived from scratch against VR-7010's exact past-timeout-cap scenario, including a control case proving the old mislabeling path was really reachable. | DONE |
| BL-0046 | 2026-08-23, VR-7010-v2 (`docs/implementation/verification/VR-7010-transport-v2.md`, finding 1) | finding | F1's fix (an explicit rejection on a nonexistent-session reconnect) also fires during a session's legitimate `'deploying'` phase in a currently-unreachable code path (no production bootstrap exists yet, BL-0038) — a pre-existing quirk, not introduced by this remediation. | Low | `07`/`08` | DEFERRED — revisit once BL-0038 (real WebSocketServer bootstrap) makes this path reachable; not blocking IP-7010's own VERIFIED result. | NEW |
| BL-0047 | 2026-08-23, VR-7010-v2 (finding 2) | doc-defect | The RTM's NFR-7200 cell doesn't yet name the new `websocketServer.test.ts` F1 regression test alongside `disconnectFlow.test.ts` — incomplete, not wrong. | Low | `07` | DEFERRED — cosmetic; rides with next RTM touch. | NEW |
| BL-0048 | 2026-08-23, VR-8010 (`docs/implementation/verification/VR-8010-presentation-ui.md`, finding F1) | bug | **High**: no message type, interface, or shared static catalog exists anywhere to deliver `AssetTemplate` data (AP cost, time-to-online) from server to client — `AssetTemplate`/content JSON live only in `server/src/content/`, GDS-09 never defines a template-catalog channel, `main.tsx` hardcodes `deployableTemplates: []`. `AssetTray`'s own render logic is independently confirmed correct in isolation (live-tested with real data) — this is a missing data-delivery path, not a component defect. Leaves FS-108 AC4/FR-8300 permanently undeliverable by the shipped system, independent of BL-0038. | High | `07-implementation-planning` (interface decision: new message type vs. shared static catalog) then `08-code-implementation` (wire it + add `AssetTray` test coverage) | SCHEDULED — next step, before `10-integration-review` can proceed (IP-8010 must reach `VERIFIED` first). | NEW |
| BL-0049 | 2026-08-23, VR-8010 (finding F2) | finding | `engageAction.ts`/`EffectResolver.resolveEngagement` (IP-4010, already `VERIFIED` via VR-4010) never check the effector's `chainRoles` for `'engage'` — only belief precision is checked. The client's `legalityPreFilter.ts` gates `engage` on `hasEngageRole`, stricter than the actual server behavior. Does not cause an NFR-4200 post-hoc-rejection failure (client only ever hides a category the server would still accept, never the reverse) — a genuine cross-package gap in IP-4010's scope, not an IP-8010 defect. | Low | `08-code-implementation`/`07-implementation-planning` (IP-4010's own scope) | DEFERRED — non-blocking; revisit alongside IP-4010's next touch or noted for `10-integration-review`. | NEW |
